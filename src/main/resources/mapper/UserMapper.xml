<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sg.edu.nus.iss.c2csectrade.mapper.UserMapper">
    <resultMap id="UserResultMap" type="sg.edu.nus.iss.c2csectrade.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="displayName" column="display_name"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="paymentPasswordHash" column="payment_password_hash"/>
        <result property="email" column="email"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="balance" column="balance"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- ResultMap with roles collection -->
    <resultMap id="UserWithRolesResultMap" type="sg.edu.nus.iss.c2csectrade.entity.User" extends="UserResultMap">
        <collection property="roles" ofType="sg.edu.nus.iss.c2csectrade.entity.Role" column="id" select="selectRolesByUserId"/>
    </resultMap>

    <select id="selectById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>
    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>
    <select id="selectByUsernameWithRoles" resultMap="UserWithRolesResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>
    <select id="selectAll" resultMap="UserResultMap">
        SELECT * FROM users
    </select>
    <select id="selectAllWithRoles" resultMap="UserWithRolesResultMap">
        SELECT * FROM users
    </select>
    <insert id="insert" parameterType="sg.edu.nus.iss.c2csectrade.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, display_name, password_hash, email, avatar_url, created_at, updated_at)
        VALUES (#{username}, #{displayName}, #{passwordHash}, #{email}, #{avatarUrl}, #{createdAt}, #{updatedAt})
    </insert>
    <update id="update" parameterType="sg.edu.nus.iss.c2csectrade.entity.User">
        UPDATE users SET username=#{username}, display_name=#{displayName}, password_hash=#{passwordHash}, payment_password_hash=#{paymentPasswordHash}, email=#{email}, avatar_url=#{avatarUrl}, balance=#{balance}, updated_at=#{updatedAt} WHERE id=#{id}
    </update>
    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id=#{id}
    </delete>

    <!-- helper select to load roles for user -->
    <select id="selectRolesByUserId" resultType="sg.edu.nus.iss.c2csectrade.entity.Role">
        SELECT r.id, r.name
        FROM roles r
        INNER JOIN user_roles ur ON ur.role_id = r.id
        WHERE ur.user_id = #{value}
    </select>
</mapper>
