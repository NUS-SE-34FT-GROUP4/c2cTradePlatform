<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sg.edu.nus.iss.c2csectrade.mapper.ViewHistoryMapper">

    <insert id="record">
        INSERT INTO pms_view_history (user_id, product_id) VALUES (#{userId}, #{productId})
    </insert>

    <select id="recentCategories" resultType="string">
        SELECT p.category
        FROM pms_view_history h JOIN pms_product p ON p.id = h.product_id
        WHERE h.user_id = #{userId} AND p.category IS NOT NULL
        GROUP BY p.category
        ORDER BY COUNT(*) DESC, MAX(h.viewed_at) DESC
        LIMIT #{limit}
    </select>

    <select id="recentProductIds" resultType="long">
        SELECT h.product_id
        FROM pms_view_history h
        WHERE h.user_id = #{userId}
        GROUP BY h.product_id
        ORDER BY MAX(h.viewed_at) DESC
        LIMIT #{limit}
    </select>
</mapper>
