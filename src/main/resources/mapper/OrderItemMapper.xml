<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sg.edu.nus.iss.c2csectrade.mapper.OrderItemMapper">

    <resultMap id="OrderItemResultMap" type="sg.edu.nus.iss.c2csectrade.entity.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="quantity" column="quantity"/>
        <result property="unitPriceSnapshot" column="unit_price_snapshot"/>
    </resultMap>

    <select id="selectByOrderId" resultMap="OrderItemResultMap">
        SELECT * FROM oms_order_item WHERE order_id = #{orderId} ORDER BY id
    </select>

    <insert id="insert" parameterType="sg.edu.nus.iss.c2csectrade.entity.OrderItem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO oms_order_item (order_id, product_id, product_name, quantity, unit_price_snapshot)
        VALUES (#{orderId}, #{productId}, #{productName}, #{quantity}, #{unitPriceSnapshot})
    </insert>

    <insert id="batchInsert">
        INSERT INTO oms_order_item (order_id, product_id, product_name, quantity, unit_price_snapshot)
        VALUES
        <foreach collection="items" item="i" separator=",">
            (#{i.orderId}, #{i.productId}, #{i.productName}, #{i.quantity}, #{i.unitPriceSnapshot})
        </foreach>
    </insert>
</mapper>
